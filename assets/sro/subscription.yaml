apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: special-resource-operator-subs
  namespace: openshift-operators
spec:
  channel: alpha
  name: special-resource-operator
  source: community-operators
  sourceNamespace: openshift-marketplace
